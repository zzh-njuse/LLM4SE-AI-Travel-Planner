# 阿里云通义千问 API 配置（可选）
# 请访问 https://dashscope.console.aliyun.com/ 获取 API Key
QWEN_API_KEY=YOUR_QWEN_API_KEY

# 可选配置
QWEN_API_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
QWEN_MODEL=qwen-turbo

# ---------------- Supabase 数据库配置 ----------------
# Supabase 将作为整个项目（本地 + 生产）的唯一数据库，请将下方占位符替换为自己项目的信息。
# 1. 登录 Supabase 控制台 → Project Settings → Database → Connection Info
# 2. 拷贝 Connection string (psql) 中的主机名和密码
# 3. 注意在 URL 末尾保留 "?sslmode=require" 以启用 SSL
# 4. 默认用户名为 "postgres"，数据库名称为 "postgres"

SPRING_DATASOURCE_URL=jdbc:postgresql://db.YOUR_PROJECT.supabase.co:5432/postgres?sslmode=require
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=YOUR_SUPABASE_PASSWORD
